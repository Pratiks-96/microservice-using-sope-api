apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "soap-service.fullname" . }}
  labels:
    app: {{ include "soap-service.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "soap-service.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "soap-service.name" . }}
    spec:
      containers:
        - name: soap-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
